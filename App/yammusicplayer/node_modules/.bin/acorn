#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@dcloudio/uni-quickapp-native/node_modules/acorn/bin/acorn" "$@"
  ret=$?
else 
  node  "$basedir/../@dcloudio/uni-quickapp-native/node_modules/acorn/bin/acorn" "$@"
  ret=$?
fi
exit $ret
